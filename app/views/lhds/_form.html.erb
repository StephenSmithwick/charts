<%= form_for(@lhd) do |f| %>
  <% if @lhd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lhd.errors.count, "error") %> prohibited this lhd from being saved:</h2>

      <ul>
      <% @lhd.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :long_name %><br>
    <%= f.text_field :long_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
